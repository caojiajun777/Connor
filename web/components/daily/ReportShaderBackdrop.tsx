"use client";

import { ReportShaderBackground } from "@/components/daily/ReportShaderBackground";

/** Client boundary for the report atmosphere (homepage-echoing CRT wash). */
export function ReportShaderBackdrop() {
  return <ReportShaderBackground />;
}
